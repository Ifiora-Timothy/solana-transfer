[package]
name = "tests"
version = "0.1.0"
description = "Created with Anchor"
edition = "2021"

[dependencies]
anchor-client ={ version ="0.30.1",features = ["async"] } 
sol-transfer = { version = "0.1.0", path = "../programs/sol-transfer" }
[dev-dependencies]
solana-program-test =  "1.18.26"